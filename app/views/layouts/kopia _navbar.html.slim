nav.navbar.navbar-expand-lg
    .navbar-collapse.text-center
        .text-center.my-navbar
            ul.navbar-nav
                li.nav-item.mr-lg-3
                    - if current_user
                        = link_to  "Sign out", destroy_user_session_path, method: :delete,  class: "btn btn-primary btn-sm mnbs"
                    - else
                        = link_to  "Sign in", new_user_session_path,  class: "btn btn-primary btn-sm mnbs"
                li.nav-item.mr-lg-3
                    - if current_user
                        = link_to 'User', user_path(current_user.id),  class: "btn btn-primary btn-sm mnbs"
                    - else
                        = link_to 'Sign up', new_user_registration_path,  class: "btn btn-primary btn-sm mnbs"
                - if can? :read, Event
                    li.nav-item.mr-lg-3
                        = link_to  "Events", events_path,  class: "btn btn-primary btn-sm mnbs" 
                - if can? :read, Option
                    li.nav-item.mr-lg-3
                        = link_to  "Option", options_path,  class: "btn btn-primary btn-sm mnbs" 
                - if can? :read, Payment
                    li.nav-item.mr-lg-3
                        = link_to  "Payments", payments_path,  class: "btn btn-primary btn-sm mnbs" 
                - if (can? :read, User) && (current_user.admin?)
                    li.nav-item.mr-lg-3
                        = link_to  "Users", users_path,  class: "btn btn-primary btn-sm mnbs" 
                - if can? :read, Post
                    li.nav-item.mr-lg-3
                        = link_to  "Posts", posts_path,  class: "btn btn-primary btn-sm mnbs" 


